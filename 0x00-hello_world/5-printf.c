#include <stdio>
/**
 * main - Print a string using, printf function
 *
 * Return: the int value of void, 0
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
		return (0);
}
