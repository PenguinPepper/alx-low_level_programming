#include "main.h"

/**
 * _islower - check for lower case characters
 * @c: The only agrument
 *
 * Return: 1 if c is lowercase and 0 if otherwise
 */
int _islower(int c)
{
	c = >= 'a' &&  <= 'z' ? 1 : 0;
}
