#include <stdio.h>

/**
 * main - print numbers of base 12
 *
 * Description: Program will print all the numbers of base 16 followed by \n
 * Return: If all goes well, 0
 */
int main(void)
{
	int num;

	char alp;

	for (num = '0' ; num <= '9' ; nu++)
	{
	putchar(num);
	}
	for (alp = 'a' ; alp <= 'f' ; alp++)
	{
	putchar(alp);
	}
	putchar('\n');

	return (0);

}
